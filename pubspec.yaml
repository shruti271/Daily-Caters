name: daily_caters
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
    
  firebase_core: ^1.5.0
  firebase_auth: ^3.0.2
  google_sign_in: ^5.0.7
  # cloud_firestore: ^2.5.0
  # cloud_firestore: 
  firebase_storage: ^10.0.2
  # firebase_core: ^1.5.0
  firebase_analytics: ^8.2.0
  dotted_border:
  dotted_line: ^3.0.0
  http: ^0.13.3
  image_picker: 


dev_dependencies:
  flutter_test:
    sdk: flutter
  
  get: ^4.3.8
    
  flutter_native_splash: ^1.2.1

flutter_native_splash:
  color: "#076800"
  image: Assets/Images/dailycaterslogofull.png
  android: true
  ios: true

flutter:
  uses-material-design: true
  assets:
    - Assets/Images/
    
